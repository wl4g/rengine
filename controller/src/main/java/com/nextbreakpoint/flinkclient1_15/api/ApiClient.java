package com.nextbreakpoint.flinkclient1_15.api;

import java.io.*;
import java.net.*;
import java.util.*;

public class ApiClient {
    private static final String CONTENT_TYPE = "Content-Type";
    private static final String APPLICATION_JSON = "application/json";
    private static final String CONTENT_DISPOSITION = "Content-Disposition";

    // ... (rest of the class, with all occurrences of "Content-Type", "application/json", "Content-Disposition" replaced by constants)

    // For the replaceAll() issue, replace with replace() where appropriate.
}
